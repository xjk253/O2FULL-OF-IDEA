{
  "name": "bubble-gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx --watch server.ts",
    "build": "esbuild server.ts --bundle --platform=node --format=esm --outdir=dist --external:ws",
    "start": "node dist/server.js"
  },
  "devDependencies": {
    "@types/ws": "^8.5.14",
    "esbuild": "^0.25.0",
    "tsx": "latest",
    "typescript": "latest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "js-yaml": "^4.1.1",
    "ws": "^8.18.2"
  }
}
